- 位运算 与& 或| 异或^ 同0异1

1. 判断n的第x位是否是1
   `(n >> x)&1 == 1`
2. 修改n的第x位为1(在这一位或1,其他或0)
   `n = n | (1 << x)`
3. 修改n的第x位为0(这一位与0,其它与1)
   `n = n & (~(1 << x))`
4. 提取n二进制下最右侧的1
   `u = n & (-n)`
5. 干掉n二进制下最右侧的1
   `u = n & (n - 1)`

- 异或运算律
  `a^0 = a`
  `a^a = 0`
